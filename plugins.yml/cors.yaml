plugins:
  - name: cors
    service: api-main
    config:
      origins:
        - https://fehura.net
        - https://sticky.fehura.net
      methods:
        - GET
        - POST
        - PUT
        - DELETE
        - PATCH
        - OPTIONS
      headers:
        - Authorization
        - Content-Type
        - X-Requested-With
      credentials: true